#include<bits/stdc++.h>
#define pi 2*acos(0.0)
#define READ(f) freopen(f, "r", stdin)
#define WRITE(f) freopen(f, "w", stdout)
#define MAX 10000000
using namespace std;

int main ()
{
    int testcase; scanf("%d",&testcase);
    while(testcase--)
    {
        string ss; cin>>ss;
        int len=ss.length(); cout<<len<<endl;
        for(i=0;i<len;i++)
        {

        }
    }
    return 0;
}
